openapi: 3.1.1
info:
  title: SPN Hub Part of Chip-in Inventory API
  version: 1.0.0
  license: 
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
servers:
  - url: https://127.0.0.1:8080
security: 
  -  bearerAuth: []
tags:
  - name: hub
    description: SPNのハブ。
paths:
  /realms/{realm}/hubs:
    $ref: './paths/hubs.yaml'
  /realms/{realm}/hubs/{hubName}:
    $ref: './paths/hub.yaml'
components:
  schemas:
    Hub:
      $ref: './schemas/Hub.yaml'
  securitySchemes:
    $ref: './components.openapi.yaml#/components/securitySchemes'